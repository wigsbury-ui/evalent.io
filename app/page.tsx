import { redirect } from "next/navigation";
export default function Home() {
  redirect("/start");
}
import { redirect } from "next/navigation";
export default function Home() { redirect("/start"); }
